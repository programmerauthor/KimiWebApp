name: Build Kimi WebView APK (Release)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Gradle
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: 8.2

    - name: Generate temporary keystore
      run: |
        rm -f app/keystore.jks
        keytool -genkey -v           -keystore app/keystore.jks           -alias release           -keyalg RSA           -keysize 2048           -validity 10000           -dname "CN=Android Debug,O=Android,C=US"           -storepass android           -keypass android

    - name: Verify project structure
      run: |
        echo "=== Project Structure ==="
        ls -la
        echo ""
        echo "=== App Directory ==="
        ls -la app/

    - name: Build Release APK
      run: |
        echo "Starting Gradle Release build..."
        gradle assembleRelease           -Pandroid.injected.signing.store.file=keystore.jks           -Pandroid.injected.signing.store.password=android           -Pandroid.injected.signing.key.alias=release           -Pandroid.injected.signing.key.password=android           --stacktrace
        echo "Build completed"

    - name: Verify APK
      run: |
        echo "=== APK Output ==="
        ls -la app/build/outputs/apk/release/
        echo ""
        echo "=== APK Info ==="
        ls -lh app/build/outputs/apk/release/*.apk

    - name: Upload APK as Artifact
      uses: actions/upload-artifact@v4
      with:
        name: KimiWebApp-Release-APK
        path: app/build/outputs/apk/release/app-release.apk
        retention-days: 90

    - name: Create/Update GitHub Release
      uses: ncipollo/release-action@v1
      with:
        artifacts: "app/build/outputs/apk/release/app-release.apk"
        tag: "v1.0.0"
        name: "Kimi WebView APK v1.0.0 (Release)"
        body: |
          🚀 Kimi AI WebView Android App (Release Build)

          ✨ Features:
          - Full WebView support for https://kimi.moonshot.cn
          - Persistent login state
          - File upload/download support
          - Camera, microphone, location permissions
          - Pull-to-refresh
          - Back button navigation

          📱 Download and install the APK to use Kimi AI on your Android device!

          ⚠️ Note: This APK is signed with a temporary development key.
        token: ${{ secrets.GITHUB_TOKEN }}
        allowUpdates: true
        replacesArtifacts: true
